import React from 'react';

// Temporarily simplified during MUI migration and missing dependencies
const AirspaceLayer: React.FC<any> = () => {
  return null; // This component is temporarily disabled
};

export default AirspaceLayer;